package InventoryManager;

import ui.MainMenu;

public class InventoryApp {
    public static void main(String[] args) {
        MainMenu menu = new MainMenu();
        menu.run(); // kick off the app loop
    }
}